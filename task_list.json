{
  "note": "ROADMAP \u2014 do in order, no skipping. Foundations done (micrograd, makemore 1-2, basic sim public). Steps t1-t4 are the WHOLE CORE: zero papers, a complete loop, and a real predict-vs-react RESULT lands at the end of t4; t5-t9 are deepening. The keystone gate is t2's plot (free-running vs teacher-forced error, vs horizon): flat -> prediction holds -> can plan (a planner; a startup); cliff -> can only react (a monitor; a commodity). Cost: t1-t7 ~$0 on the Mac, whole project <$2k \u2014 money is runway, not gear. Full per-step detail is in each task's notes; strategy / landscape / paper reading-order live in CLAUDE.md.",
  "tasks": [
    {
      "id": "t1",
      "title": "Log a dataset from the sim",
      "status": "done",
      "deps": [],
      "files": [
        "data_gen.py"
      ],
      "commit": "24a14a9f88b00b98f3b8668a1a576834b1d92cfb",
      "notes": "Many rollouts, randomized bank angles; save (state, action, next_state) tuples to a file. The sim becomes a DATA FACTORY \u2014 the training set for everything below. Do the read-step() / bank-angle tinker rep alongside (the judgment prerequisite). YOU ARE HERE."
    },
    {
      "id": "t2",
      "title": "Rung 1 \u2014 predict the next state (MLP) + the keystone plot",
      "status": "done",
      "deps": [
        "t1"
      ],
      "files": [
        "train.py"
      ],
      "commit": "547cf8fe86e13779f2599a8dec1ca0fae16206e4",
      "notes": "THE FIRST KEYSTONE. Train an MLP (state,action)->next_state, MSE. Then the money plot: roll free-running vs teacher-forced, error vs horizon \u2014 THIS PLOT IS THE GO/NO-GO GATE (flat = can plan, cliff = can only react). Fixed field, memoryless, vario-only. Zero papers (pure makemore PyTorch). Barely-a-JEPA, but the loop closes \u2014 first real milestone."
    },
    {
      "id": "t3",
      "title": "Add a planner (CEM/MPC) + navigate to a goal",
      "status": "done",
      "deps": [
        "t2"
      ],
      "files": [
        "planner.py"
      ],
      "commit": "999e1a5952467ee7dee94969a58dc4d08bb3dacd",
      "notes": "Use rung-1's predictor to imagine action sequences, score them, pick the best. TASK = REACH A GOAL, not endurance (endurance is the reactive trap \u2014 greedy 'turn toward lift' wins it; navigation forces commitment). Field still fixed (baked into weights, no memory yet). Minimal decision-forcing world: two thermals + sink between + goal past the second. Now it tries to soar."
    },
    {
      "id": "t4",
      "title": "Compare to a reactive baseline \u2014 THE EXPERIMENT",
      "status": "done",
      "deps": [
        "t3"
      ],
      "files": [],
      "commit": "15f38edd5fe1ed32b6e5f068a0c43d242c09b994",
      "notes": "Reactive 'turn toward lift' vs the planner, same task. Headline: DOES PREDICTING BEAT REACTING? \u2014 the organizing question, as a number. Visible even on a fixed field: the reactor parks in thermal A or glides blindly; the planner commits to the A->B->goal route. (When airspeed becomes a control \u2014 a v2 upgrade \u2014 add MacCready speed-to-fly as the analytical optimum to score both against.)"
    },
    {
      "id": "t5",
      "title": "Randomize + evolve the world (POMDP)",
      "status": "pending",
      "deps": [
        "t4"
      ],
      "files": [],
      "commit": null,
      "notes": "Randomize layouts + thermal drift/decay (thermals move and fade). The agent can't memorize one map \u2014 it must INFER the current field from the lift it's felt -> the encoder gets a SHORT MEMORY WINDOW (the POMDP fix). Re-run the keystone and watch it get harder \u2014 isolates difficulty-in-the-world from difficulty-in-the-glider. Drift/decay is predictable (JEPA's home turf); a thermal being born is priors-only \u2014 step 5 tests the predictable kind. BUILD THE SENSOR FIREWALL HERE."
    },
    {
      "id": "t6",
      "title": "Rung 2 \u2014 the real JEPA (latent prediction)",
      "status": "pending",
      "deps": [
        "t5"
      ],
      "files": [],
      "commit": null,
      "notes": "THE REAL JEPA. Swap predict-next-state for encode -> predict next LATENT, with EMA target encoder + stop-gradient + anti-collapse. Papers read just-in-time: BYOL -> I-JEPA -> V-JEPA 2 / 2-AC -> LeWM (full annotated list = CLAUDE.md Reading order). Re-run keystone + planner; compare latent-JEPA vs rung-1. HONEST HEADS-UP: on a low-dim state rung 2 may NOT dramatically beat rung 1 \u2014 its payoff is learning the mechanism on a safe problem + being the prerequisite for vision. Don't read a tie as failure."
    },
    {
      "id": "t7",
      "title": "Dynamic soaring (shear layer)",
      "status": "pending",
      "deps": [
        "t6"
      ],
      "files": [],
      "commit": null,
      "notes": "Same engine, swap the thermal field for a SHEAR LAYER. The albatross energy-extraction loop is a committed trajectory you can't react into -> the hard test of plan-through. Thermal centering was the gentle test; here prediction's advantage should have real teeth."
    },
    {
      "id": "t8",
      "title": "(Optional) Vision branch \u2014 add the camera",
      "status": "pending",
      "deps": [
        "t7"
      ],
      "files": [],
      "commit": null,
      "notes": "Brings a renderer (clouds/terrain) + a vision encoder (ViT/CNN) + V-JEPA-2-load onto the path. Answers: does SEEING the clouds beat inferring from the trail? \u2014 measured against the vario-only baseline. Gated behind everything above (it re-scopes the syllabus, so NOT next)."
    },
    {
      "id": "t9",
      "title": "(Optional) Real hardware \u2014 RC glider/car, inference only",
      "status": "pending",
      "deps": [
        "t8"
      ],
      "files": [],
      "commit": null,
      "notes": "RC glider + flight controller + vario/IMU/GPS, INFERENCE ONLY (training stays on the Mac). A second, filmable, real-physics datapoint. Not needed for the result. The hobby-scan flagged the RC CAR as the cheapest physical keystone if a glider is fiddly \u2014 same engine on wheels, also filmable."
    },
    {
      "id": "t10",
      "title": "Move CLIs into a cli/ folder for gen_docs auto-discovery",
      "status": "done",
      "deps": [],
      "files": [
        "cli/",
        "scripts/gen_docs.py"
      ],
      "commit": "45ad299",
      "notes": "Move CLI entrypoints into a dedicated cli/ folder so gen_docs.py globs cli/*.py instead of the hardcoded CLIS list \u2014 makes 'every CLI is documented' structural, killing the drift class the judge flagged (menu.py was silently missing from the generated ref, and docs-generated can't catch its own omission). Touches: hook paths (.githooks/, .claude/settings.json), the scripts/ test-exempt entry, land.py's sibling import of check_all, and every doc referencing scripts/<name>.py. Own branch; don't fold into other work."
    },
    {
      "id": "t11",
      "title": "land.py should commit the done-marking, not leave it dirty",
      "status": "done",
      "deps": [],
      "files": [
        "cli/land.py"
      ],
      "commit": "4392bde",
      "notes": "task.py done() only mutates task_list.json on disk; land.py step 6b never commits that edit, so main is left dirty after every land until someone happens to commit it. Surfaced when t10's done-mark got stranded uncommitted inside a concurrent session's WIP checkout. Fix: land.py commits + pushes the task_list.json update itself, atomically, inside the flock."
    },
    {
      "id": "t12",
      "title": "land.py must authorize its own done-mark commit (ALLOW_MAIN_COMMIT)",
      "status": "done",
      "deps": [],
      "files": [
        "cli/land.py"
      ],
      "commit": "9fb99a6cf0e1a48e0c94a1818fabf6172a590275",
      "notes": "t11's fix made land.py 'git commit' the done-mark on main, but the pre-commit hook refuses direct commits to main and land.py never set ALLOW_MAIN_COMMIT for itself -- so the very first real land under the t11 fix silently failed to commit (best-effort, so LANDED still printed), reproducing the exact bug t11 was fixing, one level deeper. Fix: land.py sets ALLOW_MAIN_COMMIT=1 process-wide alongside the existing LAND_ACTIVE=1, same self-authorization pattern already used for the merge hook."
    },
    {
      "id": "t13",
      "title": "Flight replay viewport (pygame) \u2014 scrub/step/play logged episodes + instruments",
      "status": "done",
      "deps": [
        "t1"
      ],
      "files": [
        "viewport.py,tests/test_viewport.py"
      ],
      "commit": "10e1eb0b3dbfa27788630667c259ec8e24bddb87",
      "notes": "REPLAY-ONLY pygame viewport for logged flight episodes (NOT realtime manual flight \u2014 that is a future task built on top of this one). Features: episode selector; play/pause + playback speed; timeline scrub; single-step forward/back; top-down view (thermal-field heatmap + flown path + glider marker + heading); instrument panel (airspeed, altimeter, vario, vario_te, bank). Scrubbing = pure array indexing \u2014 episodes are precomputed contiguous chains; NO physics runs in the viewport. DATA: data/dataset.npz (gitignored; regenerate with .venv/bin/python data_gen.py). Self-describing format: true_states (N,6: x,y,z,heading,airspeed,bank), sensors (N,8: same 6 + vario,vario_te), actions (N,2: bank_cmd,pitch_cmd), episode (N,) rollout ids, plus in-file name arrays, glider_params(+names), dt, seed. Within an episode true_next_states[i]==true_states[i+1] bit-exact. Draw the field via data_gen.make_world() (fixed-field phase) \u2014 thermal truth IS allowed for human eyes; the sensor firewall only forbids it as MODEL input. Naming convention everywhere: _cmd=asked, bare=actual, true_*=answer key (eval only). WHY (from main-session discussion 2026-07-01): (a) Mode-B silent-failure detector \u2014 watching rollouts catches what numbers hide (stalls, mush, weird arcs); (b) at t2 this gains ghost-replay: model-predicted trajectory overlaid on truth, diverging over horizon = the keystone plot as a movie \u2014 design the renderer to accept a SECOND trajectory cheaply now; (c) one log format for ALL pilots (random/planner/JEPA/human) is guaranteed by construction \u2014 every pilot flies through sim.step(bank_cmd, pitch_cmd); add a per-episode pilot-provenance tag only when a second pilot type exists (npz keys are additive, no format break). CONTEXT TRAIL: progress.txt (decision log incl. physics-fold deferrals), CLAUDE.md section Sim-v2-scoping (viewport rec: 2D pygame first, scope caution \u2014 polish on purpose, serve the JEPA), glider_sim.py module docstring (physics + panel + naming), data_gen.py docstring (schema + firewall). Left-off state: t1 (real airframe physics + data factory) green at handoff on feature/t1-dataset awaiting land greenlight. pygame must be added as a dependency (uv)."
    },
    {
      "id": "t14",
      "title": "task.py add commits id allocation to main under land.py's lock",
      "status": "done",
      "deps": [],
      "files": [
        "cli/task.py",
        "cli/land.py"
      ],
      "commit": "3d57ce1b5937f6258205552855006204da509216",
      "notes": "task.py add's _next_id() reads whatever task_list.json happens to be in the calling worktree; under worktree-per-task, two sessions adding around the same time independently compute the same next id (hit for real: two sessions both got t11). Fix: task.py add resolves task_list.json from main's actual tip (fetch/read main ref, not the local worktree copy), acquires the SAME queue.lock land.py uses so two adds can never race between read-next-id and claim-it, and commits the new entry directly onto local main (self-authorized like land.py's ALLOW_MAIN_COMMIT) -- but does NOT push, so publishing stays land.py's job on the user's greenlight. Then copies the updated file back into the calling worktree so the id is visible immediately."
    },
    {
      "id": "t15",
      "title": "task.py list hides done tasks by default",
      "status": "done",
      "deps": [],
      "files": [
        "cli/task.py"
      ],
      "commit": "69b72c1033d8117da74318b8f4b68b5f86f09387",
      "notes": "task.py list currently prints every task including done ones, growing unbounded as the roadmap progresses. Per user preference (2026-07-02): hide done tasks from the default listing; -v/--full (already the 'show more' flag) also brings them back into view. rehydrate.py already only shows a done COUNT, not individual done tasks -- no change needed there. The header progress bar / counts stay based on the full task list regardless of what's filtered from the per-row listing."
    },
    {
      "id": "t16",
      "title": "Wingtip lift-asymmetry sensor \u2014 the bird gradient cue (before t2)",
      "status": "done",
      "deps": [
        "t1"
      ],
      "files": [
        "glider_sim.py,data_gen.py,tests/test_glider_sim.py,tests/test_data_gen.py"
      ],
      "commit": "1fab010",
      "notes": "sense() gains lift_asym = updraft(left wingtip) - updraft(right wingtip): the instantaneous LATERAL lift gradient \u2014 the torque cue birds center thermals with (validated: Reddy et al. PNAS 2016 + Nature 2018 \u2014 torque cue essential, vario-only much weaker). Glider gains wingspan (~17 m, ASK-21); wingtip horizontal separation scales with cos(bank) (knife-edge = no lateral sampling). Firewall-legal: field sampled at aircraft-attached points \u2014 felt, never told. Point-mass dynamics UNCHANGED (cue is sensed, not a simulated torque). SENSOR_NAMES grows to 9 \u2014 data_gen packs off SENSOR_NAMES so the factory adapts automatically; regenerate data/dataset.npz after landing. Sign: positive = left wing in stronger lift = turn left. Why before t2: first dataset the MLP sees should carry the bird sensor; also gives t4's reactive baseline the honest bird rule (turn toward the lifting wing) so predict-vs-react is fair. NOTE: t13 viewport notes say sensors (N,8) \u2014 stale; format is self-describing, read sensor_names from the file."
    },
    {
      "id": "t17",
      "title": "task.py notes command \u2014 edit a task's notes after creation",
      "status": "done",
      "deps": [],
      "files": [
        "cli/task.py"
      ],
      "commit": "32b963b9e5d40f5d21c56464551716ccfa8ecf55",
      "notes": "add/block/done can't fix a typo or add context to an existing task's notes without ALLOW_STATE_EDIT. Add 'task.py notes <id> (--set TEXT | --append TEXT)' following start/done/block's local-save pattern (no main-commit needed, land.py reconciles)."
    },
    {
      "id": "t18",
      "title": "agentic-workflow.md: add notes to the per-command bullet list",
      "status": "done",
      "deps": [],
      "files": [
        ".claude/rules/agentic-workflow.md"
      ],
      "commit": "ced240f",
      "notes": "AI review on t17 flagged: the 'When to use each command' prose list enumerates every task.py subcommand except notes (added in t17). Docs aren't wrong elsewhere -- generated CLI block + scripts/README.md already cover it -- just this one inventory bullet."
    },
    {
      "id": "t19",
      "title": "task.py done accepts pending, not just active \u2014 land.py's real fix",
      "status": "done",
      "deps": [],
      "files": [
        "cli/task.py"
      ],
      "commit": "6be838c9a739760dd9dffa45de988cd5ace7a2d8",
      "notes": "land.py's done-mark step (task.py done <id> --commit <sha>) requires status==active, but the local active flag lives ONLY in the primary checkout's uncommitted working tree -- it's wiped by any ordinary git checkout/dirty-cleanup on primary between start and land (hit for real landing t18: had to discard task_list.json to satisfy land's pre-merge dirty-check, which then made the post-merge done-mark fail with 'only an active task can be marked done', requiring a manual ALLOW_MAIN_COMMIT catch-up commit). Fix: cmd_done accepts status in (active, pending) -- land.py's branch-name-derived task binding is the real proof of legitimacy, not the ephemeral local flag. Keep rejecting blocked/done (idempotency, no silent unblock)."
    },
    {
      "id": "t20",
      "title": "land.py auto-reconciles a dirty task_list.json/progress.txt instead of refusing",
      "status": "done",
      "deps": [],
      "files": [
        "cli/land.py"
      ],
      "commit": "6ab7bc494631bc3e845798f4017411c9534960b5",
      "notes": "land.py's precondition check hard-refuses on ANY dirty file in the primary checkout, including task_list.json/progress.txt left locally dirty by task.py start / log.py (neither commits). This causes recurring 'main worktree is dirty' failures requiring manual git checkout/reset before every land -- easy for a future session to forget. Fix: if the ONLY dirty files are task_list.json and/or progress.txt, land.py self-authorizes (ALLOW_MAIN_COMMIT=1, same pattern as its done-mark) a small commit absorbing them before proceeding with the merge. Any OTHER dirty file still hard-refuses -- this only widens the exception for the two files task.py/log.py already own."
    },
    {
      "id": "t21",
      "title": "land.py reconcile: don't auto-commit a deleted task_list.json/progress.txt",
      "status": "done",
      "deps": [],
      "files": [
        "cli/land.py"
      ],
      "commit": "e1f618d37de4b10ab33b76d08d1692b1f6d70ab0",
      "notes": "AI review on t20 flagged: _reconcile_dirty_state accepts any porcelain status on the two state files, including deletion (' D task_list.json'), which git add would stage and auto-commit to main. Scaffold's own tools only ever produce modifications; a deletion signals something went wrong and should fall back to the hard-refuse branch. Fix: parse the porcelain status code, only reconcile M/A (modified/added), not D."
    },
    {
      "id": "t22",
      "title": "task.py begin \u2014 claim + create the worktree in one step",
      "status": "done",
      "deps": [],
      "files": [
        "cli/task.py"
      ],
      "commit": "688071179e252a455bcf2081383cd745a8d00083",
      "notes": "Right now claiming a task is 3 manual steps: task.py start tN, hand-derive a slug + git worktree add ../soaring-tN -b feature/tN-<slug> main, then cd. Add task.py begin tN: does start's claim, derives the branch slug from the task title automatically, creates the worktree, and prints the cd command to run next. True teleportation isn't possible (a subprocess can't cd the parent shell), so this collapses it to 'run one command, then cd' instead of 3 manual steps. task.py start stays available standalone for off-convention cases."
    },
    {
      "id": "t23",
      "title": "guard_state.py blocks hand-edits to tracked files in the primary checkout",
      "status": "done",
      "deps": [],
      "files": [
        "scripts/guard_state.py"
      ],
      "commit": "67937b49b1496ab641c28ed9e96fec1800567033",
      "notes": "Direct Edit/Write to a tracked source/doc file in PRIMARY was only conventionally discouraged, not hook-enforced -- caught for real when I hand-edited agentic-workflow.md directly in primary for a t17 docs nit and had to self-catch + redo as t18. Extend guard_state.py: task_list.json/progress.txt keep their existing per-file guard (any checkout). NEW: any OTHER tracked, non-gitignored file is also blocked when the CURRENT checkout is the PRIMARY one (git rev-parse --git-dir == --git-common-dir), not a linked task worktree -- coding in a worktree is unaffected, that's the whole point. Gitignored files (CLAUDE.md, .venv, data/) stay unguarded. New break-glass: ALLOW_MAIN_EDIT=1, mirroring ALLOW_MAIN_COMMIT."
    },
    {
      "id": "t24",
      "title": "guard_state.py doc wording: not gitignored, not tracked",
      "status": "done",
      "deps": [],
      "files": [
        "scripts/guard_state.py,scripts/README.md,.claude/rules/agentic-workflow.md"
      ],
      "commit": "c0328aa4d22d4748d3fc814d45b93c210b855117",
      "notes": "AI review on t23 flagged: docstring/block message/docs describe the primary-checkout rule as guarding 'tracked, non-gitignored' files, but the code actually blocks ANY non-gitignored in-repo path -- including a brand-new untracked file via Write. Blocking new-file creation in primary is the right call (new work belongs on a worktree too), so keep the behavior; just reword every 'tracked' reference to 'not gitignored' so the docs match reality."
    },
    {
      "id": "t25",
      "title": "Sim stall fidelity: hysteresis + mush ramp (kill the tick-rate stall limit cycle)",
      "status": "pending",
      "deps": [
        "t2"
      ],
      "files": [
        "glider_sim.py"
      ],
      "commit": null,
      "notes": "Found flying the t13 viewport: binary stall flag + dt=0.1 => ~5 Hz stall/recover limit cycle when commanded speed < banked stall speed (IAS pins at Vs(bank), vertical speed square-waves +/-3 m/s). Fix ladder: (1) hysteresis band \u2014 stall below Vs, recover only above Vs + ~1.5 m/s; (2) replace binary stall_sink with a continuous mush ramp near/below Vs (real gliders mush, and smooth dynamics are friendlier to the t2 MLP); (3) optional/later: pitch/AoA state so the phugoid emerges. Deps t2 on purpose: sim stays FROZEN until the keystone plot lands so results are comparable. Regenerate data/dataset.npz after; ledger entry 2026-07-02 has full context. | SCOPE ADD (2026-07-03, user-found): pitch response is a step not an ease-in \u2014 vertical speed from the energy exchange is ~V*accel/G and jumps to full value on tick 1 (derivative of a rate-limited airspeed ramp = step), while bank eases via roll_rate. Fix alongside stall hysteresis/mush: either pitch/AoA state (ladder item 3, phugoid emerges) or the cheap rung \u2014 second-order airspeed (lag the acceleration itself) so vertical speed eases in/out. See progress.txt 2026-07-03 entry."
    },
    {
      "id": "t26",
      "title": "gitattributes: merge=union for progress.txt",
      "status": "done",
      "deps": [],
      "files": [
        ".gitattributes"
      ],
      "commit": "e813a73280a1bf5a65d787528fa3b3ea729d92c1",
      "notes": "t13's landing prep hit a real progress.txt merge conflict when merging main into the feature branch (main gained t17-t24 appends while t13 was in flight), resolved ad hoc by hand. No .gitattributes exists yet, so every future branch will hit the same tail-append conflict and get resolved inconsistently -- risky if anyone reaches for a whole-merge -X union instead (that would silently union-merge ANY conflicting file, mangling real code conflicts). Fix: commit .gitattributes with 'progress.txt merge=union' (git's builtin union merge driver, no custom script needed) so this conflict class auto-resolves safely and consistently on every merge, not just land.py's primary-checkout dirty-tree case (t20/t21, which is a different code path)."
    },
    {
      "id": "t27",
      "title": "Viewport ghost-compare: save keystone rollouts + load multiple trajectory files side by side",
      "status": "done",
      "deps": [
        "t2",
        "t13"
      ],
      "files": [
        "keystone.py,viewport.py,tests/test_keystone.py,tests/test_viewport.py"
      ],
      "commit": "8ddca0479357ab8e2c065d51803e08c1770cd70d",
      "notes": "GOAL (user, 2026-07-02): compare the model's imagined rollouts against the real flights they shadow, inside the replay viewport \u2014 more than one trajectory on screen at once, side by side, synchronized scrubbing. TWO HALVES, one feature. (A) PERSIST THE ROLLOUTS: keystone.py currently computes its runs dict in main() and discards it \u2014 add a savez_compressed to data/rollouts.npz, self-describing like dataset.npz: sensor_names, dt, horizon, starts (n,) absolute row indices into dataset.npz, episode (n,) of each start, true (n,H+1,9) answer-key slice, and one array per predictor (rollouts_full, rollouts_twin, rollouts_teacher_forced; skip persistence \u2014 trivial). Rollout rows align 1:1 in time with dataset rows from each start (same dt 0.1), h=0 row == the true start row \u2014 a test should pin rollouts_full[:,0]==true[:,0] and true[:,h]==sensors[starts+h]. Regenerate anytime via .venv/bin/python keystone.py (deterministic). (B) VIEWPORT MULTI-SOURCE: CLI file args (e.g. viewport.py data/dataset.npz data/rollouts.npz) or in-app picker; render N trajectories either side-by-side panes or ghost-overlaid in one pane (t13 amendment already asked the renderer to accept a second trajectory cheaply \u2014 this is that, generalized). One shared playback clock: a rollout's local step h maps to dataset row starts[i]+h, so true flight and imagined flight scrub in lockstep. Label IMAGINED vs TRUE loudly in the UI (different color/dash; the imagined panel is model belief, not physics). Instruments work unchanged \u2014 rollout arrays are 9-channel panels in sensor_names order. CONTEXT TRAIL: keystone.py docstring (rollout mechanics, free-running vs teacher-forced), progress.txt 2026-07-02 entries (keystone verdict FLAT: position 14.6 m @ 15 s vs persistence 352 m; twin vario diverges 5.7->34 m/s \u2014 SHOWING that divergence in the viewport is the money demo), data/keystone.png + ghost_paths.png (the static version of what this task animates). FIREWALL: not an issue here \u2014 rollouts are model output, thermal heatmap background is truth-for-human-eyes only (t13 precedent). NOT IN SCOPE: the three judge-hardening items on keystone.py (rollout_starts slice fix, split assert, predict_delta helper) \u2014 t3's first commit owns those; coordinate via land.py if both sessions touch keystone.py. Torch note: run everything with the shared primary .venv (worktrees have none)."
    }
  ]
}
