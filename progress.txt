2026-06-30  [- 861dd54]  Agentic setup scaffolded: deterministic gate (check_all, 9 gates), AI reviewer (review.py), serialized merge queue (land.py), and durable session-state tooling (task.py + guard hook + rehydrate + log.py). Roadmap seeded into task_list.json (t1..t9). Next up: t1 — log a dataset from the sim.
2026-07-01  [t1 fe18662]  t1 started — plan: data_gen.py = rollout loop (piecewise-constant random bank, fixed single-thermal field, seeded RNG) -> .npz of (state, action, next_state, vario); firewall structural (row builder can't see Thermal, no thermal params in file); tests: shapes, determinism, physics-consistency vs fresh sim.step, firewall assertion.
2026-07-01  [t1 fe18662]  t1 paused mid-work (user request). Written: data_gen.py + tests/test_data_gen.py; pyproject mypy files += data_gen.py; .gitignore += data/. NOT yet run: ruff format, check_all, commit. Resume: format -> gate -> commit on feature/t1-dataset -> handoff summary (no land without greenlight).
2026-07-01  [t1 fe18662]  Decision (user, overrides earlier 2-lever-at-v2 framing): v1 glider gets the FULL real-world control set — PITCH joins bank as a live control (up/down + speed<->height energy exchange, realistic aero formulas). Only operating details (rudder/trim coordination) stay abstracted. Implies: airspeed becomes STATE not constant, energy accounting, glide polar, glider MASS (Glider currently has none — only airspeed + base_sink). To be scoped as its own task; changes dataset format (actions 2-D, state gains airspeed). revisit: when scoping the physics-upgrade task, before t2.
2026-07-01  [t1 f144b22]  t1 physics fold complete + dataset generated (120k rows, 200 eps). Deferrals logged: overspeed = command clamp NOT airframe breakup (revisit: if planner needs real fear); g-meter sensor skipped (derivable from bank; revisit: never, probably); vario needle lag + sensor noise deferred to t5 POMDP package; air-density-vs-altitude deferred to v2. Note: dataset contains stalls but ZERO crashes (spawns >=300m can't drain to 0 in 60s) — fine for t2 keystone, revisit if a landing/endurance task appears. fly.py headline survives real physics: +100.7/-64.1 (was +206/-91 in toy — bigger turn radius at real speeds sweeps more of the field; verified consistent by hand).
2026-07-01  [t10 e81f0c5]  t10 started — plan: move CLIs into cli/ folder so gen_docs auto-discovers them
2026-07-01  [t10 e81f0c5]  t10: moved CLIs (task/land/review/log/rehydrate/menu/gen_docs) into cli/; gen_docs.py now globs cli/*.py instead of a hardcoded list; check_all is a self-check via ALLOW_EXEMPT=1 (cli/ exempted like scripts/ was)
2026-07-01  [t10 7a00031]  t10: standardized worktree-per-task as the default (not just for parallel sessions) -- land.py now auto-cleans the branch's worktree + branch on success
2026-07-01  [t10 c9b5a1e]  t10: fixed a real bug caught while landing -- land.py resolved .venv from its own worktree instead of the shared repo, crashing mid-gate the first time land ran from a fresh worktree; rolled main back and added a crash-safety net + regression tests
2026-07-02  [t1 0453b17]  t11 id collision fixed: dropped our uncommitted-to-main viewport t11 (reset f04a2ca), re-merged main (clean — brought t11/t12 land.py done-mark fixes + gate tests), re-added viewport as t13 (same context-complete notes). Exempt-guard merge-base fix stays routed to the scaffolding session. t1 branch remains green at handoff, now current with main through t12.
2026-07-02  [- 92c442e]  [ledger 2026-06-30] Sim refactored procedural -> objects. glider_sim.py is now Glider (airframe params + sink_rate/turn_rate), ThermalMap (thermals + base wind), Simulation (the world: holds glider+air+state+dt, owns step()/sense(), stores history). Thermal/GliderState unchanged. Physics identical to the verified v1 (same Euler step). Reason: pluggable glider/world + the sim becomes a data factory. fly.py updated to match.
2026-07-02  [- 92c442e]  [ledger 2026-06-30] Glider params fed to the MLP (accepted proposal 1) -- INERT until gliders are randomized. Wiring airframe params (glide ratio, sink, ...) as MLP inputs makes it learn p(next | state, action, glider). But steps 1-4 train on ONE glider -> those inputs are constant, absorbed into weights exactly like the fixed thermal field. 'Fed glider params' != 'generalizes across gliders' until the data-gen loop varies the airframe. revisit: when randomizing gliders (~step 5).
2026-07-02  [- 92c442e]  [ledger 2026-06-30] Base wind on ThermalMap, default (0,0), advection wired but unexercised. step() advects the glider by the wind vector (correct physics, no-op at 0). It's the structural hook for thermals drifting with the wind and for the shear layer. revisit: step 5 (drift/decay) + step 7 (dynamic soaring).
2026-07-02  [- 92c442e]  [ledger 2026-06-30] dt + airspeed now live on Simulation/Glider (were module-level constants). Tunable per-sim / per-airframe now.
2026-07-02  [- 92c442e]  [ledger 2026-06-30] Real airframe physics deferred: glide polar + energy state + stall/crash. v1 fakes the airframe with two constants (airspeed, base_sink) -> the glide polar collapses to one point. Future upgrade (sim v2, when airspeed becomes a CONTROL not a constant): a real glide polar (sink-rate-vs-airspeed map) -> glide ratio / min-sink / best-glide speeds, plus wing loading, polar coefficients, stall speed, mass, and a real energy state (kinetic <-> potential exchange -- trade speed for height and back). With that comes the consequence v1 can't have: a glider can run out of energy and stall / fall out of the sky (fly too slow -> stall; bleed all altitude AND speed -> crash). This makes endurance/energy a hard constraint, not just a score, and gives the planner real stakes. These are the inert-until-randomized airframe inputs proposal-1 wires but can't exercise yet. revisit: sim v2 / whenever airspeed becomes a control (parts of steps 4-7).
2026-07-02  [- 92c442e]  [ledger 2026-06-30] Sensor firewall NOT yet built (the silent-cheat guard). Once thermals hide (step 5), the MLP-input builder must see ONLY sense() + own kinematics + glider params + action -- NEVER the thermal's true (x0,y0,w_peak,radius). sense() (returns local vario) is the legitimate channel; the input builder must be structurally unable to reach the Thermal objects. revisit: when writing the data-gen / training file (step 1-2).
2026-07-02  [t13 65d828f]  t13 started (worktree ../soaring-t13). SCOPE AMENDED by user (2026-07-02): (1) manual arrow-key flight + flight-log recording FOLDED INTO t13 (no new task) — arrows = bank_cmd/pitch_cmd, record via Simulation.history -> rows_from_rollout -> same npz schema; (2) FULL 3D viewport, user decision overriding the '2D pygame first' scoping note — stack: ursina/Panda3D, pure-Python, PROCEDURAL geometry only (vertex-colored terrain = the heatmap, translucent thermal columns, path ribbons, primitive glider; NO assets/textures = the rabbit-hole guard). Top-down ortho camera mode replaces the 2D analysis view; renderer takes N trajectories (t2 ghost-replay slot). Panel = name-keyed gauge registry + generic fallback for unknown channels (robust to sensor growth); lift_asym gauge per the t13 amendment note. revisit: if ursina fights the gate (mypy/headless), fall back to pygame+hand-rolled 3D projection.
2026-07-02  [t13 65d828f]  t13 RENDERER DEAD-END (documented so nobody retries it): ursina 8.x CANNOT render on this Mac. Root causes, each verified by probe: (1) macOS GL supports GLSL 120 (legacy 2.1) or 150+ (3.2 core) ONLY; ursina's shaders are hard-coded GLSL 130/140 -> compile-fail -> black entities. (2) Patching versions 130/140->150 via Shader.compile hook fixes OUR shaders but ursina's unlit_with_fog fragment samples p3d_Texture0 UNCONDITIONALLY and Apple GL returns BLACK for unbound samplers -> untextured entities black (ursina's own textured exit button renders fine -> confirmed). (3) Even with a custom clean GLSL-150 untextured shader, an anonymous GLSL-130 shader still compiles AT IMPORT TIME (before any patch can install) and 3D stays black while textured UI renders. Layers 1-2 fully diagnosed, layer 3 unattributable without forking ursina. Decision point: pivot renderer, keep architecture (frames/panel/colors/app state machine all pure + tested, renderer-agnostic by design). revisit: only if ursina ships GLSL-150 shaders or metal support.
2026-07-02  [t13 8780869]  t13 BUILD COMPLETE, branch green, awaiting land greenlight. Delivered: viewport/ package (camera/colors/frames/panel/scene/hud/app) — 3D vector viewport (pygame), REPLAY (scrub/step/speed/episodes/click-seek) + FLY (arrow keys: left-right=bank in sim convention, up-down=speed setpoint; S saves dataset-schema npz to data/flights/, seed=-1=human) + 3 cameras (chase/topdown-ortho-heatmap/tower). Robustness contract tested: panel + frames are name-keyed off the npz's own channel arrays, unknown sensors get a generic gauge automatically; ghost-trajectory slot (app.ghosts + draw_path flat_color) ready for t2. 122 tests, check_all green, verified end-to-end with a real window (frames captured: gauges, needles incl. lift_asym, climb-colored ribbon, nav-light chirality all correct; flew 5s manual left-bank spiral, physics + HUD consistent). Primary venv gained pygame (hook resolves venv from primary). NOTE: pygame replaced ursina in pyproject; dataset regen needed nothing (schema untouched, zero sim-logic changes).
