2026-06-30  [- 861dd54]  Agentic setup scaffolded: deterministic gate (check_all, 9 gates), AI reviewer (review.py), serialized merge queue (land.py), and durable session-state tooling (task.py + guard hook + rehydrate + log.py). Roadmap seeded into task_list.json (t1..t9). Next up: t1 — log a dataset from the sim.
2026-07-01  [t1 fe18662]  t1 started — plan: data_gen.py = rollout loop (piecewise-constant random bank, fixed single-thermal field, seeded RNG) -> .npz of (state, action, next_state, vario); firewall structural (row builder can't see Thermal, no thermal params in file); tests: shapes, determinism, physics-consistency vs fresh sim.step, firewall assertion.
2026-07-01  [t1 fe18662]  t1 paused mid-work (user request). Written: data_gen.py + tests/test_data_gen.py; pyproject mypy files += data_gen.py; .gitignore += data/. NOT yet run: ruff format, check_all, commit. Resume: format -> gate -> commit on feature/t1-dataset -> handoff summary (no land without greenlight).
2026-07-01  [t1 fe18662]  Decision (user, overrides earlier 2-lever-at-v2 framing): v1 glider gets the FULL real-world control set — PITCH joins bank as a live control (up/down + speed<->height energy exchange, realistic aero formulas). Only operating details (rudder/trim coordination) stay abstracted. Implies: airspeed becomes STATE not constant, energy accounting, glide polar, glider MASS (Glider currently has none — only airspeed + base_sink). To be scoped as its own task; changes dataset format (actions 2-D, state gains airspeed). revisit: when scoping the physics-upgrade task, before t2.
2026-07-01  [t1 f144b22]  t1 physics fold complete + dataset generated (120k rows, 200 eps). Deferrals logged: overspeed = command clamp NOT airframe breakup (revisit: if planner needs real fear); g-meter sensor skipped (derivable from bank; revisit: never, probably); vario needle lag + sensor noise deferred to t5 POMDP package; air-density-vs-altitude deferred to v2. Note: dataset contains stalls but ZERO crashes (spawns >=300m can't drain to 0 in 60s) — fine for t2 keystone, revisit if a landing/endurance task appears. fly.py headline survives real physics: +100.7/-64.1 (was +206/-91 in toy — bigger turn radius at real speeds sweeps more of the field; verified consistent by hand).
2026-07-01  [t10 e81f0c5]  t10 started — plan: move CLIs into cli/ folder so gen_docs auto-discovers them
2026-07-01  [t10 e81f0c5]  t10: moved CLIs (task/land/review/log/rehydrate/menu/gen_docs) into cli/; gen_docs.py now globs cli/*.py instead of a hardcoded list; check_all is a self-check via ALLOW_EXEMPT=1 (cli/ exempted like scripts/ was)
2026-07-01  [t10 7a00031]  t10: standardized worktree-per-task as the default (not just for parallel sessions) -- land.py now auto-cleans the branch's worktree + branch on success
2026-07-01  [t10 c9b5a1e]  t10: fixed a real bug caught while landing -- land.py resolved .venv from its own worktree instead of the shared repo, crashing mid-gate the first time land ran from a fresh worktree; rolled main back and added a crash-safety net + regression tests
